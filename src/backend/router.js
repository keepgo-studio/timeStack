const {
  setDarkTheme,
  setSystemTheme
} = require("./controller");

function direct(port, { data }) {
  let statusCode = 0;
  const { command } = data;

  // else if (){

  // }
}


module.exports = direct;